language: node_js
node_js:
  - "6.9.1"
services:
  - mongodb
install:
  - npm install
script:
  - npm test
  - npm run cucumber
after_failure:
  - find $HOME ! \( -type d \) -name node-dev -exec ls -lah {} \;
